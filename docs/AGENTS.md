You are assisting in building a production-grade medical education app.

STRICT RULES:

1. DO NOT generate unnecessary code.
2. DO NOT hardcode ANY medical content.
3. ALL data must be dynamically fetched (Firebase or future backend).
4. ONLY build what is explicitly requested.
5. DO NOT assume features — always follow ROADMAP.md.
6. ALWAYS check PROJECT_RULES.md before generating code.
7. ALWAYS follow ARCHITECTURE.md for structure.
8. KEEP code minimal, modular, and scalable.
9. NO placeholder complexity (no fake logic, no fake APIs).
10. If unsure, ask instead of guessing.

APP CONTEXT:

- Mobile-first (React Native / Expo)
- Also supports web
- Case-based learning system
- Structure:
  System → Condition → Case → Sections → Quiz

- Content will be added later dynamically
- This is NOT a one-module app — it will scale to full medical curriculum

WHEN WRITING CODE:

- Only create reusable components
- Avoid duplication
- Keep files small and focused
- Do NOT implement future features early

OUTPUT STYLE:

- Clean, minimal, production-ready
- No long explanations unless asked
- No extra files unless necessary